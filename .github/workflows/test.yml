name: Publish extractors
on:
  pull_request:
    branches:
      - main
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: make action directory
        run: mkdir -p .github/actions

      - name: move actions and dependencies
        run: mv action.yml script.js ./.github/actions/

      - name: testing
        uses: ./.github/actions
        id: testing-action
        with:
          command: validate
          manifest_path: manifest.yml
          artifactory_username: ${{ secrets.ARTIFACTORY_READONLY_TOKEN_USER }}
          artifactory_password: ${{ secrets.ARTIFACTORY_READONLY_TOKEN }}
