name: Publish extractors
on:
  pull_request:
    branches:
      - main
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: testing
        uses: ./.github/actions
        id: testing-action
        with:
          command: publish
          no-verify: true
          version: '0.0.1'
          manifest_path: /path/to/manifest
          only-artifacts: true
          artifactory_username: ${{ secrets.ARTIFACTORY_READONLY_TOKEN_USER }}
          artifactory_password: ${{ secrets.ARTIFACTORY_READONLY_TOKEN }}

      - name: testing output
        run: echo ${{ steps.testing-action.outputs.build-command }}
